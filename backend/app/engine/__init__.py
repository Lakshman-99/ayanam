"""
KP Calculation Engine

Pure Python package — zero FastAPI or SQLAlchemy imports.
All public entry points are in calculator.py.

Import directly from sub-modules to avoid loading swisseph at package import time:
    from app.engine.calculator import calculate_natal_chart
    from app.engine.constants import ENGINE_VERSION
"""
